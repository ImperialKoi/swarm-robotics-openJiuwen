# SHIPPING.md

**Generated by `swarmmind/training/gate.py`. Do not edit by hand.**

<!-- current-selection -->
## Current defaults versus historical gate results

Unflagged `Mission` defaults: `evolved=True`, `command=False`, `unit_policy=None`, `zone_routing=False`, `response_team=None`. The tables below record evaluation outcomes; they do not override later integration decisions. Zone routing remains off after M-76f; its Tier-3-off win did not establish a win for the live demo. The classical detector ships; M-74 records the later CNN evaluation. The response team is an opt-in application, without a rescue-uplift claim. The operator
voice channel (`--voice`) and the team's visual scout (`--team-scout`) are the same: both
are opt-in applications, both default off, and **neither has been through this gate**.
No rescue-score improvement is claimed or measured for either. They are interfaces and an
extra observation, not performance changes; see
[docs/OMNI_VOICE.md](docs/OMNI_VOICE.md) and M-92/M-93. See [current build and demo instructions](docs/MULTI_AGENT_DEMO.md).

The [Nepal confluence crop](docs/NEPAL_TERRAIN.md) changes map geometry even for the same demo seeds. Gate results from the previous terrain do not establish performance on this crop; components remain tuned on the four demo maps used at the time of their training.
<!-- /current-selection -->

What actually ships, per component, and by what margin. A classical component winning is
a **result**, not a failure to hide -- that is the whole reason this file is generated
from measurements rather than written from intent.

Every comparison below runs on the demo's four maps (`demo.yaml` `demo_seeds`), because
those are the only maps the demo plays. Components trained elsewhere -- the bodies (test
fixture, seeds 11-13) and the detector (seeds 1-8) -- have never seen them. **The unit policy
was trained on these same four maps**: its margin is how much better it is *on the demo*,
not evidence that it works anywhere else. The mission score is
`10*rescued + 2*found + 5*ground_explored - 0.5*lost`.

Scenario `demo`, seeds `[42, 43, 44, 45]`, 4 missions per arm, margin 1.05x.

## Summary

| component | ships | margin |
|---|---|---:|
| Tier-2 robot bodies (MAP-Elites) | **evolved roster** | 1.31x |
| Tier 3 hivemind | **scripted rung** | 1.05x |
| Tier 3a commander | **no commander** | 0.98x |
| Perception (CV detector) | **classical detector** | not run |
| Tier-1 zone routing | **zone routing** | 1.19x |
| Tier-2 unit policy | **zone routing** | 1.04x |

## Tier-2 robot bodies (MAP-Elites)

MAP-Elites evolved the bodies (`radius`, `v_max`, `omega_max`, `sensor_radius`, `battery_capacity`); `select.py` picks the extremes of measured speed per lane rather than the top-N by fitness, so the roster is deliberately diverse. **The genome's behaviour half is not shipped** -- the bid weights in play are the hand-tuned classical ones in both arms, so this comparison isolates bodies.

| arm | score | rescued | found | ground explored | lost |
|---|---:|---:|---:|---:|---:|
| classical | **652.25** | 49.00 | 96.25 | 95.1% | 70.00 |
| evolved roster | **856.26** | 68.75 | 93.00 | 95.3% | 44.00 |

> evolved roster ships (1.31x baseline)

## Tier 3 hivemind

The rung measured here is the **scripted** baseline, rung 4 of the ladder -- no model has been trained or run against the demo scenario. It is the number a tuned model has to beat, not a claim about one.

| arm | score | rescued | found | ground explored | lost |
|---|---:|---:|---:|---:|---:|
| evolved roster | **856.26** | 68.75 | 93.00 | 95.3% | 44.00 |
| Tier 3 scripted | **902.21** | 72.50 | 97.00 | 96.7% | 43.25 |

> Tier 3 scripted ships (1.05x baseline)

## Tier 3a commander

Decided on ten held-out maps, three arms: none 70.81 / heuristic 65.57 / trained 69.42. `command=False` is the default and the code stays so the claim is reproducible. Full record in `training_notes/SUMMARY.md`; do not reopen by tuning `CommandParams`.

> no commander ships; trained reached 0.98x, under the 1.05x bar

## Perception (CV detector)

Perception is real either way: robots see through a detector running on their own occluded camera frame, and only the *trained* label is at stake. The CNN runs forward in numpy (`perception/cnn.py`) so nothing here depends on torch; it costs ~46 ms per 500-frame pass against the classical detector's 5.5 ms, which is the price it has to earn back. Train it with `swarmmind/training/notebooks/detector.ipynb` on Kaggle. Cuttable per PLAN §7.1 item 4.

> no trained detector at assets/models/detector.npz; `perception/classical.py` ships

## Tier-1 zone routing

Loaded carriers descend an exact fine-grid field to the nearest collection point, over ground eroded by one cell so no route is narrower than a robot can hold a heading through (`control/zone_routing.py`, MEASUREMENTS M-76). A correctness fix, not a trained component.

| arm | score | rescued | found | ground explored | lost |
|---|---:|---:|---:|---:|---:|
| evolved roster | **856.26** | 68.75 | 93.00 | 95.3% | 44.00 |
| zone routing | **1015.24** | 83.75 | 97.50 | 97.3% | 44.25 |

> zone routing ships (1.19x baseline)

## Tier-2 unit policy

Per-robot staging for carriers and diggers with nothing urgent (`control/unit_policy.py`), trained by behaviour cloning from the heuristic then PPO on Kaggle (`training/rl/`). **Trained on these same four demo maps** -- a win here means a win on the demo, not on maps it has not seen. The bar is the better of routing alone and the heuristic.

| arm | score | rescued | found | ground explored | lost |
|---|---:|---:|---:|---:|---:|
| zone routing | **1015.24** | 83.75 | 97.50 | 97.3% | 44.25 |
| heuristic staging | **987.45** | 80.75 | 96.75 | 96.6% | 36.75 |
| trained unit policy | **1056.97** | 87.50 | 97.00 | 96.8% | 33.75 |

> zone routing ships; trained unit policy reached only 1.04x, under the 1.05x bar
