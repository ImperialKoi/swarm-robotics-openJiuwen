# Copy to .env and fill in. .env is gitignored; load it with `uv run --env-file .env ...`.
OPENROUTER_API_KEY=
# Optional: any OpenRouter model with strict structured outputs (default openai/gpt-4.1-mini).
# OPENROUTER_MODEL=openai/gpt-4.1-mini
